everything is working fine, if by extracting pincode the city found in master_db is not matching with input_db you have to flag it and it will also go under the ambigous case and give both the city names, similar logic will go for every other part of flow chart
